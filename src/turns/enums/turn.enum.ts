export enum ELocationType {
  'COUNTRY' = 'country',
  'CITY' = 'city',
  'SCENE' = 'scene',
  'PORT' = 'port',
  'TURN' = 'turn',
}

export enum EPlanType {
  // ST_AsText query
  'TEXT' = 'text',
  // ST_MakeLine query
  'LINE' = 'line',
}
